import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact page",
  description: "This is contact page",
};

export default function ContactPage() {
  return <span className="text-5xl">ContactPage</span>;
}
