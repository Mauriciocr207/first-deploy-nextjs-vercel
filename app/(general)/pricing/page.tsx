import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing page",
  description: "This is pricing page",
};

export default function PricingPage() {
  return <span className="text-5xl">PricingPage</span>;
}
